<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        android:id="@+id/app_bar_tracker"
        layout="@layout/app_bar_tracker"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        app:itemShapeFillColor="@color/selector_menu"
        app:itemShapeAppearanceOverlay="@style/ShapeAppearanceOverlay.Nav"
        android:theme="@style/ThemeOverlay.NavigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/_60sdp"
        android:layout_marginBottom="@dimen/_56sdp"
        android:layout_gravity="start"
        android:background="@drawable/nav_drawer_bg"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/tracker_nav_header_main"
        app:menu="@menu/tracker_activity_main_drawer" />
</androidx.drawerlayout.widget.DrawerLayout>